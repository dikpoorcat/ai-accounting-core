<!-- @format -->

# 仓库专项约束

## 工作模式路由

- 用户要求修改代码、测试、迁移、部署、文档或产品设计时，按开发模式处理，并遵守本文件后续约束。
- 用户单独说“启动”，或要求启动记账环境、做好记账准备时，使用仓库 Skill `$accounting-startup`：准备本地 SQLite 常驻服务、验证 MCP 与负责人登录并打开页面。仅准备环境，不自动开始业务处理；“增加／修改启动指令”等实现请求仍属于开发模式。
- 用户要求处理真实企业的票据、流水、工资、税务、资产、借款、更正、月结，或说“开始记账”时，进入会计操作模式并使用仓库 Skill `$accounting-operator`；该模式默认只处理业务资料和调用 `ai_accounting` MCP，不修改仓库实现。
- 会计操作模式的正式业务规则以 `finance_local_schema.agent_operating_protocol` 为准；本文件只负责模式路由，不复制运行契约。
- 用户的意图同时包含产品开发和真实会计操作，且先后顺序会影响正式数据时，先明确当前要执行的模式，不把开发授权扩大为业务写入授权。

## 核心业务约束

- 本地仓库是唯一的主开发仓库，不设副本仓库。
- 本仓库是确定性记账内核。不得新增允许智能体提交任意借贷分录的 API。
- 入账金额以整数“分”为单位。仅税率和税额计算使用 `Decimal`，绝不使用二进制浮点数。
- 正式凭证必须借贷平衡；正式写入必须保持原子性和幂等性。
- 未关账月份允许通过类型化业务修改入口原子重算原凭证，或通过删除入口撤去无后续依赖的误记业务；保留原编号及完整审计记录；已关账凭证不可变，更正必须创建关联冲正凭证。
- 业务事实缺失时必须返回 `needs_information`；不得引入会改变会计处理的推断性默认值。
- 税收规则必须按生效日期版本化，并附首要官方来源 URL。
- 当前用于一位本地负责人管理多家公司；每家公司使用独立业务数据库，且每个业务库只含一家企业。它不是法定账簿、纳税申报或报税系统。
- 测试与验证范围按用户当前指令和改动风险确定，不设每次修改都必须执行的统一全量门禁。

## 记账内核

- 公共入口只接收类型化业务事实，不接收任意科目、借贷方向或自由分录行。
- 入账金额使用整数分；税率和税额计算使用 `Decimal`，不使用二进制浮点数。
- 正式凭证必须借贷平衡；正式写入保持原子性和幂等性。
- 未关账月份通过类型化业务修改入口重算原凭证，或删除无后续依赖的误记业务，并留存审计历史；已关账凭证不原地修改，更正创建关联冲正凭证。
- 缺少会改变会计处理的事实时返回 `needs_information`，不采用推断性默认值。
- 新增或调整事实必填项、精度及阻断规则时，同步声明字段核算／管理语义和可复用来源；新内核复用 `kernel.types` 的年月、实际日期和金额类型，核算缺项或冲突使用结构化 `fact_issues` 说明实际字段、允许精度及约束。错误码不得直接转换成用户追问；测试应覆盖省略管理资料、不同允许精度、来源复用及原子失败，不能用测试辅助函数把管理日期复制成核算日期。
- 税收规则按生效日期版本化，并记录首要官方来源 URL。

## 当前定位

- 当前是一位本地负责人管理多家独立数据库公司的确定性记账内核，不宣称是法定账簿、纳税申报或报税系统。
- 具体模块范围和现有接口以代码及 README 的能力说明为准；它们是当前实现状态，不是不可更改的永久产品决定。
- 验证范围按当前任务和风险选择，不设每次改动必须执行的固定全量测试矩阵。

## 本次重构与数据库版本

- 本次按“全新开发、全新库”设计，可复用现有可用实现，不受旧数据库、旧接口或旧数据格式约束。主设计见 [整体架构](docs/kernel-refactor-architecture.md)，阶段顺序、46 项审查对应关系及状态见 [路线图](docs/kernel-refactor-roadmap.md)。原审查保留问题依据，其中要求承接现库的建议已由本次范围决定替代。
- 当前代码仍为目录库 v3、公司库 v13，原阶段 1、2 已在此前提下完成；新版本体系尚未实现。这是当前实现记录，不是要求后续继续沿旧版本追加迁移。本次文档修订不修改代码、结构合同 JSON 或真实数据。
- 新系统的目录库、公司库、结构合同和备份包必须有明确的新体系标识，并核对类别、结构版本及完整指纹。不能只把版本数字重新叫作 v1。旧目录 v1–v3、公司 v1–v13、已记录 v10 特例和旧备份不再作为新系统升级或恢复来源。
- 基础修订负责新体系及通用运行层，各业务阶段清理所属领域的旧兼容。旧合同不改写成新合同，按计划退出运行时与运行包；源码历史由 Git 保留。不得按 `legacy`、`old` 或 `v2` 名称批量删除正常业务能力。
- 开发期使用明确的开发基线，仍核对包内合同、库内记录和 SQLite 实际保存的 SQL 原文。同结构可正常进行登记、关账、备份和恢复测试；结构变化后显式建立新的临时合成库，不承诺阶段间测试库升级，不自动删除、重建或补标识。已有文件即使没有业务行，也不是可覆盖的新建目标。
- 第 9 阶段交付时冻结目录库和公司库各自的首个正式版本；正式版不承诺接收开发库。此后的已发布合同不可回写，结构变化必须走同体系内明确的前向迁移。保留 `schema_history`、完整结构指纹、父合同约束的增量合同、事务及数据保留核验；基础修订即以隔离测试合同验证未来升级和失败回滚。
- 新系统从首版保存完整的必要历史采用依据，缺失即错误，不保留仅为旧记录缺依据而存在的有限核验例外。今后升级不得用当前计算覆盖冻结历史。期初、累计工资、跨月清偿、无影响复核、无凭证结果、开放期替换和闭期冲正仍是正常业务能力。
- 不可变业务源与可重建目录、投影分别处理。目录只索引源中实际引用，不推导采用或完成结论；普通连接不全库扫描或自动回填。维修实际改变结果时原子推进读取修复版本，它不属于第四个业务 lane。
- 允许直接修改接口，不维护新旧两套；同阶段更新 CLI、MCP、页面、生成合同、测试和操作说明。未改动的当前入口仍以代码为准，不得把已批准目标写成已实现功能。
- 默认入口为 `finance-local` 和其 MCP 适配，本机 Python 使用仓库 `.tmp-kernel-venv`。测试以新建合成公司、明确业务预期及保留的纯函数测试为依据，不以旧公司余额一致或旧阶段通过数作为新基线验收。
- 本次不包含真实资料重录、现库升级、旧备份导入、旧业务转换或正式换库。现有数据库、保全原件、负责人确认和备份均不得作为旧资源清理；原已完成的恢复、身份接续和资源退役记录不授权进一步操作真实资料。

## 原始资料保全

- 旧回放文档仅作为已保全事实的来源索引，不再提供旧内核运行入口。原件与负责人补充确认保存在忽略的 `data/reconstruction-sources`；具体公司身份、金额和凭据不进入源码。

## 初始备份交付

- 用户要求生成公司“初始备份”时，每家公司必须在其已配置的关账备份目录中独立生成 `<统一社会信用代码>.finance-company.zip`，经过新内核便携包验证后才算完成；不得把多家公司合并进同一个包。
- `backup` 命令返回持久任务；只有 `jobs` 中成功结果指向已验证 ZIP 才能交付，pending 或中间 SQLite 文件不能算完成。首次备份只有当前版 ZIP，后续滚动才产生 `.previous.finance-company.zip`。

## Git 格式

- 每阶段按“细化设计 → 实施 → 验证 → 修正 → 更新文档 → 提交 Git → 停止”执行。代码、测试和文档一起提交，记录实际结果及提交状态，不自动进入下一阶段；纯文档阶段只提交文档。
- 建议 Git 提交信息使用中文。需要详细说明时，可以在简洁标题后空一行，再用 `1、...`、`2、...` 说明实际改动，其间禁止空行。
- 除开发主机交接外，不强制重复检查分支、上游、ahead/behind 或工作区；其他 Git 操作以用户当次指令为准。

## 前端开发

- 凡涉及前端页面、Vue 组件、前端路由、状态管理、样式、交互或前端 API 调用的任务，在开始修改前必须阅读并遵守仓库根目录下的 `FRONTEND.md`。
- 如果 `AGENTS.md` 与 `FRONTEND.md` 对前端开发存在更具体的规则冲突，前端相关事项优先遵守 `FRONTEND.md`。

## 资料完整性

- 月度清单、逐项来源和处置版本只追加；关账和完整代发使用同一检查器，不能用文件引用或旧完成标记绕过未处理行、隐藏数据、金额/期间差异和失效结果。
- 公司说明通过 `company_context` / `update_company_note` 保存版本；原始 `<统一社会信用代码>/业务说明.md` 作为来源保全，不放入源码。说明不代替正式事实与不可变证据。
- `payment_period`、`payment_category` 仅用于归集，不能替代核算所属期或改变凭证。

## 文档编辑

- 文档不要以补充说明的方式写，读起来负担太重。直接改写文档，保留有用的信息即可。
