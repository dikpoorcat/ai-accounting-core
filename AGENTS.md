<!-- @format -->

# 仓库专项约束

## 工作模式路由

- 用户要求修改代码、测试、迁移、部署、文档或产品设计时，按开发模式处理，并遵守本文件后续约束。
- 用户要求处理真实企业的票据、流水、工资、税务、资产、借款、更正、月结，或说“开始记账”时，进入会计操作模式并使用仓库 Skill `$accounting-operator`；该模式默认只处理业务资料和调用 `ai_accounting` MCP，不修改仓库实现。
- 会计操作模式的正式业务规则以 `finance_get_event_schema.agent_operating_protocol` 为准；本文件只负责模式路由，不复制运行契约。
- 用户的意图同时包含产品开发和真实会计操作，且先后顺序会影响正式数据时，先明确当前要执行的模式，不把开发授权扩大为业务写入授权。

## 核心业务约束

- 本地仓库是唯一的主开发仓库，不设副本仓库。
- 本仓库是确定性记账内核。不得新增允许智能体提交任意借贷分录的 API。
- 入账金额以整数“分”为单位。仅税率和税额计算使用 `Decimal`，绝不使用二进制浮点数。
- 正式凭证必须借贷平衡；正式写入必须保持原子性和幂等性。
- 已过账凭证不可变。更正必须创建关联冲正凭证。
- 业务事实缺失时必须返回 `needs_information`；不得引入会改变会计处理的推断性默认值。
- 税收规则必须按生效日期版本化，并附首要官方来源 URL。
- 当前用于一位本地负责人管理多家公司；每家公司使用独立业务数据库，且每个业务库只含一家企业。它不是法定账簿、纳税申报或报税系统。
- 测试与验证范围按用户当前指令和改动风险确定，不设每次修改都必须执行的统一全量门禁。

## 记账内核

- 公共入口只接收类型化业务事实，不接收任意科目、借贷方向或自由分录行。
- 入账金额使用整数分；税率和税额计算使用 `Decimal`，不使用二进制浮点数。
- 正式凭证必须借贷平衡；正式写入保持原子性和幂等性。
- 已过账凭证不原地修改；更正创建关联冲正凭证。
- 缺少会改变会计处理的事实时返回 `needs_information`，不采用推断性默认值。
- 税收规则按生效日期版本化，并记录首要官方来源 URL。

## 当前定位

- 当前是一位本地负责人管理多家独立数据库公司的确定性记账内核，不宣称是法定账簿、纳税申报或报税系统。
- 具体模块范围和现有接口以代码及 README 的能力说明为准；它们是当前实现状态，不是不可更改的永久产品决定。
- 验证范围按当前任务和风险选择，不设每次改动必须执行的固定全量测试矩阵。

## 数据库基线

- `0001_formal_baseline` 是试用结束时由最终结构冻结的正式空数据库基线；正式库启用后的结构变化必须通过后续前向 revision 迁移，不得回写基线或用未跟踪的现场脚本代替迁移。
- 不支持从已删除的试用 revision 原地升级。对包含正式业务数据的数据库执行迁移前，必须先完成备份和只读前置检查；发现未知历史库时先停止迁移并由用户另行决定处理方式。

## Git 格式

- 建议 Git 提交信息使用中文。需要详细说明时，可以在简洁标题后空一行，再用 `1、...`、`2、...` 说明实际改动（正文间禁止空行）。
- 除开发主机交接外，不强制重复检查分支、上游、ahead/behind 或工作区；其他 Git 操作以用户当次指令为准。

## 前端开发

- 凡涉及前端页面、Vue 组件、前端路由、状态管理、样式、交互或前端 API 调用的任务，在开始修改前必须阅读并遵守仓库根目录下的 `FRONTEND.md`。
- 如果 `AGENTS.md` 与 `FRONTEND.md` 对前端开发存在更具体的规则冲突，前端相关事项优先遵守 `FRONTEND.md`。
