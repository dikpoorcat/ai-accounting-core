# 前端开发规范

本文件是项目的**前端专用开发规范**。

凡涉及前端页面、Vue 组件、样式、交互、前端状态、路由、前端 API 调用等工作，在开始修改代码前必须先阅读并遵守本文件。

本项目属于个人维护的中小型项目。前端开发的首要目标是：

> **简单、清晰、稳定、容易修改。**

不要为了所谓的“工程化”“可扩展性”或假设中的未来需求，引入当前项目并不需要的复杂架构。

---

## 1. 技术栈

除非现有项目已经明确使用其他方案，否则前端统一使用：

- Vue 3
- Vite
- TypeScript
- Composition API
- `<script setup lang="ts">`
- Vue Router
- Pinia：仅在确实需要全局或跨页面共享状态时使用

不得在没有明确技术必要性的情况下：

- 更换前端框架
- 引入其他路由库
- 引入其他状态管理库
- 引入与现有技术栈功能重复的大型框架或库

优先沿用项目已有技术方案，而不是重新选择一套自己偏好的方案。

---

## 2. Vue 组件规范

优先使用 Vue 单文件组件（`.vue`）。

统一使用：

```vue
<script setup lang="ts">
</script>

<template>
</template>

<style scoped>
</style>
```

开发时遵循以下原则：

- 组件自己的模板、逻辑和样式尽量放在同一个 `.vue` 文件中。
- 优先修改已有组件，而不是立即创建新的抽象层。
- 简单逻辑不要单独创建组件、Composable、工具函数或 Store。
- 不要为了“代码看起来更工程化”而过度拆分组件。
- 一个组件只有在确实过大、职责明显过多、难以理解，或者存在真实复用需求时才进行拆分。
- 不要提前为尚不存在的需求设计抽象。
- 一个只使用一次、逻辑简单的 UI 区块通常没有必要单独拆成组件。

拆分组件的目的是**降低复杂度**，而不是增加文件数量。

---

## 3. 状态管理

状态应尽量放在离使用它最近的位置。

优先级通常为：

1. 组件内部状态
2. Props / Emits
3. Composable
4. Pinia

如果状态只属于一个组件，优先保留在组件内部。

简单的父子组件通信优先使用：

- `props`
- `emit`
- `v-model`

Pinia 只用于真正需要：

- 多个无直接父子关系组件共享
- 多个页面共享
- 跨路由保留
- 应用级统一管理

的状态。

不得仅仅为了“统一架构”就把局部状态放入 Pinia。

避免：

- Store 和组件同时维护同一份状态
- 多份数据互相同步
- 为简单状态设计复杂的数据流

应尽量保持**单一数据来源**。

---

## 4. Composable 使用原则

`src/composables` 用于真正可复用的 Composition API 逻辑。

不要把所有逻辑都包装成 `useXXX()`。

只有在以下情况之一成立时，才优先考虑创建 Composable：

- 多个组件需要复用同一套逻辑
- 单个组件中的某块逻辑已经明显复杂
- 独立出来后能明显降低组件理解成本

如果只是几行简单逻辑，直接留在组件中通常更好。

不要为了“代码漂亮”制造大量只有几十行甚至几行代码的 Composable。

---

## 5. API 请求

前端与后端通信相关代码原则上集中放在：

```text
src/api/
```

组件中不要重复实现相同的请求逻辑。

增加新的 API 调用前：

1. 先检查 `src/api` 中是否已经存在对应接口。
2. 优先复用现有 API 函数。
3. 确认确实不存在后再新增。

组件主要负责：

- 用户交互
- 页面状态
- 调用 API
- 展示 API 返回结果

不要在组件内部重复编写大量：

- URL 拼接
- 请求参数转换
- 通用错误解析
- 相同的请求封装

但同时也不要为了一个非常简单的请求设计过度复杂的：

```text
Controller
↓
Service
↓
Repository
↓
Adapter
↓
API Client
```

对于本项目而言，通常没有必要。

---

## 6. TypeScript

新增前端代码默认使用 TypeScript。

重要业务数据应定义明确的类型。

类型尽量放在其所属业务模块附近，而不是无条件全部塞入一个全局 `types.ts`。

只有多个无关模块都需要使用的真正公共类型，才考虑放入：

```text
src/types/
```

遵循：

- 尽量避免 `any`
- 不为了消除 TypeScript 报错而随意使用类型断言
- 不使用复杂泛型解决简单问题
- 不设计没有实际需求的通用类型系统
- 优先保证类型容易阅读和修改

TypeScript 的目的是减少错误，不是增加代码复杂度。

---

## 7. 依赖管理

新增第三方依赖前，必须先检查：

1. 项目是否已经有相同或类似功能的依赖。
2. Vue / 浏览器原生能力是否已经可以简单完成。
3. 是否真的值得为了这个功能增加长期依赖。

不得为了几十行可以清晰实现的简单功能随意引入大型库。

尤其不要未经必要性判断就新增：

- UI 框架
- 状态管理库
- CSS 框架
- 工具函数库
- HTTP 请求库
- 表单框架
- 日期库

如果已有依赖可以完成，应优先复用已有依赖。

---

## 8. 项目目录

首先遵循项目当前已有目录结构。

新增模块时，一般按照以下方式组织：

```text
src/
├─ api/          # 后端 API 调用
├─ components/   # 可复用 UI 组件
├─ composables/  # 可复用 Composition API 逻辑
├─ router/       # Vue Router
├─ stores/       # Pinia Store
├─ types/        # 真正跨业务共享的类型
├─ utils/        # 小型、通用、可复用工具函数
└─ views/        # 页面 / 路由级组件
```

不要为了一个功能随意新增：

```text
services/
repositories/
adapters/
controllers/
managers/
providers/
factories/
core/
domain/
infrastructure/
```

等新的架构层。

只有当前项目确实出现相应复杂度时，才考虑新增。

---

## 9. 修改现有代码

实现需求时，应优先选择：

> **能够完整解决问题的最小改动。**

遵循：

- 保留项目已有结构和代码习惯。
- 不要顺手重构与当前任务无关的代码。
- 不要为了个人偏好的代码风格重写已经正常工作的代码。
- 不要因为看到旧代码“不够优雅”就进行大范围调整。
- 不得将用户已有、但与当前任务无关的修改覆盖或删除。
- 修改前先理解现有实现，再决定修改方式。
- 能在现有实现基础上完成，就不要重新实现一套。
- 本次修改导致旧代码明确不再需要时，应删除对应废弃代码。

如果发现值得重构、但不属于当前任务的问题，可以指出，但不要擅自扩大修改范围。

---

## 10. 简单优先原则

本项目是个人维护的中小型项目。

不要按大型企业项目的规模进行架构设计。

尤其避免无实际需求地引入：

- 设计模式
- 多层架构
- Repository Pattern
- Factory Pattern
- Dependency Injection
- Service Container
- Event Bus
- 通用框架封装
- 大量 Base Class
- 复杂泛型体系
- 复杂配置驱动系统

如果两个方案都能解决问题，优先选择：

1. 文件更少
2. 代码更少
3. 依赖更少
4. 数据流更直接
5. 状态来源更清晰
6. 更容易阅读
7. 更容易调试
8. 更容易由人和 AI 在未来继续修改

不要针对目前不存在的需求进行设计。

例如：

> “以后可能支持十种实现，所以现在先做一个通用 Provider + Adapter + Factory。”

如果目前只有一种实现，就先实现这一种。

等真正出现第二种、第三种实现时，再根据实际需求抽象。

---

## 11. 避免过度抽象

不要看到两段代码相似就立即抽象。

只有当重复代码已经：

- 明显增加维护成本
- 容易导致多个地方修改不一致
- 形成稳定、明确的共同逻辑

时，才考虑抽象。

一个抽象如果比被抽象掉的代码更难理解，就不要做。

优先：

```text
简单重复
```

而不是：

```text
难以理解的通用抽象
```

特别是在业务仍然快速变化的阶段。

---

## 12. 避免 AI 式代码膨胀

开发过程中不要为了完成一个简单需求主动制造大量新的：

- 文件
- Interface
- Type
- Class
- Wrapper
- Helper
- Hook / Composable
- Store
- Service
- Config

实现一个功能前先判断：

> 是否能直接在现有代码中清晰地完成？

如果答案是可以，就优先直接修改现有代码。

例如，一个简单功能原则上不应该自动演变成：

```text
Component
↓
Composable
↓
Store
↓
Service
↓
Adapter
↓
Utility
```

除非每一层都有明确、实际的职责。

---

## 13. 样式与 UI

优先沿用项目已有的样式体系。

不要在同一个项目中混用多套 CSS 方案。

如果项目已有：

- 普通 CSS
- Scoped CSS
- CSS Modules
- Tailwind
- UI 组件库

应优先继续使用已有方案。

不要仅仅为了实现几个样式而新增另一套 CSS 框架。

组件专属样式优先放在组件自己的：

```vue
<style scoped>
```

中。

真正的全局样式才放入全局样式文件。

---

## 14. 前端与后端边界

本项目为前后端同仓库项目。

修改前端功能时：

- 不要无必要修改后端代码。
- 不要因为前端实现不方便就擅自修改后端接口。
- 如果现有后端 API 无法合理支持需求，应先明确指出问题。
- 如果确实需要同时修改前后端，应保证接口定义、字段和调用方同步修改。

不得只修改其中一端，留下已经明确不兼容的接口。

---

## 15. 开始开发前

执行前端任务前：

1. 阅读本文件。
2. 查看项目现有目录结构。
3. 查看 `package.json`。
4. 确认项目已经使用的框架和依赖。
5. 查看与本次任务最相关的已有组件、Store、API 和类型。
6. 优先理解现有实现，再开始修改。

不要仅根据任务描述直接重新设计一套实现。

---

## 16. 修改完成后的检查

完成前端代码修改后，应检查：

- Vue Template 是否存在错误
- TypeScript 是否存在错误
- import 是否正确
- 是否存在未使用代码
- 是否存在明显重复实现
- 是否意外影响其他页面
- 是否引入了不必要的依赖
- 是否留下已经废弃的旧代码

如果项目已有对应命令，应执行：

```bash
npm run lint
```

以及：

```bash
npm run type-check
```

最后必须执行：

```bash
npm run build
```

如果 `package.json` 中实际使用的是其他等价命令，应使用项目已有命令。

由本次修改导致的构建错误必须修复。

不得通过以下方式单纯隐藏问题：

- 使用 `any`
- 使用 `@ts-ignore`
- 关闭 TypeScript 检查
- 删除校验规则
- 屏蔽错误

除非确实存在明确的技术原因。

---

## 17. 最终原则

进行前端开发时，始终优先考虑：

> **当前需求是什么？**

而不是：

> **未来这个项目理论上可能变成什么？**

对于本项目：

**清晰的简单代码，优于复杂的“最佳实践”。**

**能够稳定工作的直接实现，优于为了架构而设计的抽象实现。**

**方便下一次继续修改，优于一次性设计一个看起来完美的系统。**

当无法确定应该选择简单方案还是复杂方案时：

> **默认选择简单方案。**