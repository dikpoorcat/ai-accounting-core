# 本地 SQLite 内核验收记录

2026-09-11，最终新内核测试 **274 项全部通过**，无跳过，耗时 150.42 秒。
验收使用隔离的合成公司；未迁移或切换真实公司和已安装 MCP。

| 验收部分 | 已验证内容 |
|---|---|
| 确定性 | 严格整数金额和月份、固定规则及程序内容版本、显式缺项、不可变计算上下文 |
| 事务与封存 | 借贷平衡、延迟外键、封存后修改拒绝、幂等、过期预览、真实 COMMIT 失败回滚 |
| 更正 | 空范围来源后补、累计工资联动、实际付款保护、开放期保号、闭期关联冲正、无影响复核、删除依赖 |
| 查询与关账 | 汇总清空重建逐分一致、冻结历史、资料覆盖、月末业务义务、季度三表、外部提交依据复核 |
| 文件与恢复 | 银行代发、三表、Online Backup 完整公司包、恢复校验、任务失败及中断后的重试 |
| 运行入口 | CLI、真实 STDIO MCP、本地鉴权页面、公司与月份切换、凭证与来源追溯、移动布局 |

Ruff、Git 差异空白检查、锁文件检查及前端类型检查/生产构建均通过。
最终程序内容版本为 `local-kernel-1:8840d89e6e5f3696c43518715d67a4a85bed2d155ac909dee8c0846c7a1ea1b5`，Python 3.12.13 / SQLite 3.53.1。
源码与运行环境统计见 [机器可读验收记录](local-kernel-acceptance.json)。

独立软件包在另一目录实际解压，使用包内解释器与依赖验收；详见
[运行包说明](local-kernel-packaging.md)及[运行包验证记录](local-kernel-package-verification.json)。
构建内容版本应与本页一致。

性能测量单列于 [凭证规模测试](local-kernel-benchmark.md)、
[工资与密集更正](local-payroll-benchmark.md)、[事实批量登记](local-source-batch-benchmark.md)。
这些报告保留各自源码与数据库结构摘要，不能把开发过程中较早的测量冒充最终代码的新测量。
百万凭证是简单费用场景；完整季度报表、冷启动及长期并发容量不包含在该数字内。

旧报表模板提取保留了既有报表服务全部 23 个方法及模板函数的 AST，兼容模板用例通过。
本次没有宣称运行并通过整个旧 PostgreSQL 测试套件。
