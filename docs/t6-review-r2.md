# T6 架构复审修订 r2

任务 `01a095d6-05dd-7c63-82eb-d29543bf6c48`，架构审核任务 `01a09416-a4e3-76b2-bf46-6387d8d47cfc`。依据架构对结果 v1 的两项明确修订放行；沿用批准计划 v1，不扩大范围。

原 [v1 结果](t6-implementation-result.md)、[v1 验证 JSON](t6-implementation-verification.json) 和所有基线/日志/截图均保留。v1 验证 JSON SHA256 仍为 `70a5a03e6a46d30f1d0a350e52899b1a8745dd0354697489fe0e65fdea0945aa`。修订前原件另存 `.tmp/t6-r2/baseline`，本轮记录见 [r2 验证 JSON](t6-review-r2.json)。

## 两项修正

| 审核项 | 修正与结果 |
| --- | --- |
| R1 / P2：报表跨公司沿用旧季度 | 页头切公司仅传公司与共享月份，清除旧季度及报表资料选择。新 context 核对同月；没有同月时采用该公司的 default_period，由报表现有同步逻辑从最终月份确定季度。A 一月/Q1 → B 可查看六月、三月且默认六月，现在采用六月/Q2。保留原 App 对同公司明确季度深链的 fallback，不修改或删除该项回归。 |
| R2 / P3：旧刷新提前改写新刷新完成提示 | 删除旧续页在 `await refresh()` 后的提示写入。成功/首页失败提示由实际首页请求的世代及控制器检查决定；context 失败由该次 refresh 的世代检查决定。九→十→九后，旧刷新结束不再把新刷新误称为已完成；新 context 返回但首页仍未返回时继续显示正在读取，直到当前首页完成或失败。 |

生产实现仅改 `DashboardModuleHeader.vue` 与 `FundsView.vue`。App、ReportsView、共享 context、核算含义、查询及数据库均未改。测试复用现成真实脚本 harness；浏览器验证器只增加独立窄场景，保留旧场景入口。

## 本轮验证与适用性

- R1 定向回归先在旧实现复现三月/六月错误及旧季度残留；修后 Node 统计 6 项通过、7 项按名称跳过。覆盖跨公司默认月、同月仍可用、空公司和页头清理，并保留同公司明确季度 fallback 的原有测试通过。
- R2 先在旧实现复现迟到提示：资金文件 9 通过、1 失败。修后 10 项通过；新回归验证旧刷新不能结束新提示、context 完成不等于首页已读取、最新首页成功以及 context/首页失败的重试提示。
- 只运行一次 r2 普通构建，类型检查及 Vite 构建通过。没有重跑 62 项前端全套、五页完整浏览器矩阵、内核、历史成本或包链。
- 窄浏览器场景使用 3 条类型化合成费用事实建立 A 一月和 B 六月/三月，校验真实 B context 的月份及默认月。登录报表 A 一月/Q1 后通过公司选择器切换 B，核对 URL、公司/季度控件、月份回退提示、同源季度报表响应和实际请求 JS 正文哈希。截图仅保留切换前后两张。

实际 UI 复验通过：最终 URL 为 B / 2026-06 / 2026-Q2，明确提示“所选月份已不可查看，已切换至 2026-06。”；控件与真实 API 一致。本次只加载 4 个 JS，正文哈希全部匹配最终构建，运行期间源码变更为空，控制台/API 失败为空。两张截图已人工检查：[切换前](../.tmp/t6-r2/browser/report-company-a-q1.png)、[切换后](../.tmp/t6-r2/browser/report-company-b-default-q2.png)。stderr 保留 3014 字节既有 P3 断连堆栈，不声称服务端零错误。

保留不同批次的原始结果，不合并统计。r2 没有布局或 CSS 修改；v1 首轮架构已认可的五页视觉、历史/当前语义、来源及其他交互证据继续适用，v1 的实际 JS 哈希仍只指向其原构建。本轮记录补充修改后源码、最终构建全部静态文件及实际浏览器加载文件哈希。

测试服务显式读取本轮 `frontend/dist`，使用仓库虚拟环境及全新合成 root，最后停止服务、撤销/删除测试凭据；不使用安装服务、不操作真实业务资料或部署。T5 沿用的客户端断连日志 P3 保持原非阻塞取舍，与本次资金页提示 R2/P3 是两件不同的问题。

两项修订与本轮证据交架构审核；roadmap/task-prompts 由架构任务维护，本任务不创建后续任务。
