# T5「整体验证与交付收口」计划 v1

任务：01a095aa-d868-7bb2-a092-0ff76cc90108
架构审核任务：01a09416-a4e3-76b2-bf46-6387d8d47cfc
状态：架构已批准 v1 及文末六项执行边界；用户已要求在默认模式开始。

## 1. 承接成果、代码基线与剩余风险

承接已审核 T1 核算等价及完整精确来源、T2 内容采用与提交版本分离、T3 统一业务查询/独立状态、T4 r2 的 v10 引用目录、有界读取及五页接入。目录库保持 v3、公司库 v10。保守 unestablished/null、有限期初采用证明、完整相关分类校验、真实历史现金汇总及完整冻结图均沿用已批准边界。

规划基线 HEAD 为 66561ee07afb2841562c269a9c60f320787d17e3，但前序成果尚有未提交/未跟踪文件，HEAD 不能单独标识交付。已将 T4 r1 的最终清单按 r2 覆盖后核对 81 个路径，79 个一致；仅架构负责的 roadmap/task-prompts 在审核后变化，代码与测试无额外漂移。

本次补齐四类缺口：
- T4 改动 engine/workflow/versions/共享读取后，T1 补证—复核—闭期—真实更正链和 T2 经营说明内容链缺少当前状态的统一验证。
- 当前 HTTP 测试使用占位 HTML；服务优先使用 packaged 静态目录，而 T4 build 更新的是 frontend/dist，两者 JS 产物不同。旧浏览器脚本仍有 v1 mock，不能代表当前五页。
- T4 r1 成本记录的 business_queries 已被 r2 修改，且未完整登记 reports 等依赖；r2 恢复的完整分类校验需要单独量化，不能将旧数字改称当前测量。
- 部分现行说明仍写“公司 v9”或“新公司格式第 1 版”，历史 v8 软件包及旧验收记录须与当前源码、T5 软件候选包分清。

## 2. 去重后的统一回归与集成

先建立 T5 基线/证据清单：保留全部既有改动，记录实际源码、前端、测试、迁移合同、构建配置及锁文件 SHA256；将每项风险标为“复用有效证据”或“本次运行”，写明旧证据适用/失效原因。T1/T2 忽略目录中的必要结论、记录摘要和哈希纳入可交付 JSON，不搬运原始长日志。

统一 Python 回归只运行下列去重节点，一次 pytest 调用，不运行全仓库或整个 tests/kernel：
- test_accounting_review_chain.py、test_review_no_impact.py。扩展现有工资链测试，在补证前后、正式无影响复核和闭期真实更正节点联查 business_status、period_readiness、简报/员工/资金及凭证 trace；检查原冻结不变、无重复金额、当前后续事项与历史核算独立。不另建重复工资链。
- test_accounting_compatibility.py 中 comparison_failure_is_atomic_isolated_and_recoverable、failed_no_impact_review_keeps_old_publication_and_can_retry。既有全库 dump 已覆盖 v10 目录，验证失败回滚、隔离及幂等重试。
- test_commentary_content.py。仅在既有“无关未来写入”和“冻结原文/后补说明”场景补页面与共同查询断言，覆盖内容仍有效但旧提交拒绝、相关依据失效、逐字段来源和冻结/后补分区。
- test_workflow_confirmation_time.py 中 idempotent_replay_and_recalculation_keep_original_knowledge_time、changed_basis_keeps_actual_completion_and_requests_review、missing_confirmation_audit_keeps_unknown_day_unestablished。
- test_exports.py::test_any_lane_change_expires_unconfirmed_export、test_materials.py::test_changed_or_deleted_current_result_reopens_original_row，验证等价/内容有效没有放松材料及文件办理的精确依据。

以上均使用仓库 .tmp-kernel-venv，实际完整 nodeid/参数化节点写入运行记录。复用 T1 纯核算投影矩阵、T2 已有效字段溯源证据、T3 r1/r2 缺陷闭环、T4 请求世代/分页/渲染、r2 三项缺陷语义回归、48 组合凭证差分及迁移/完整性故障矩阵；不为得到一个更大的通过数量重跑。

补一个当前产物的集成闭环：
- 一次 npm run build:release（已含类型检查），构建当前页面；一次 scripts/package-local-kernel.ps1 默认完整移目录验证，不使用 SkipValidation。产物放新的 T5 专属 .tmp-local-distribution 目录，名称及 SHA256 绑定本次代码状态，只作为待审核软件候选包。
- 复用 scripts/verify_local_package.py 现有合成费用、后台测试备份和恢复链，最小增加 CLI、恢复后 CLI、真实 STDIO MCP 的 business_status/period_readiness 查询一致性；HTTP 检查四页 v2、context v2、quarterly-report/business-status v1、整数分字符串、集合计数与准备投影。确认 v9/v10 迁移文件及新增模块进入包内。合成备份只属于测试产物，不是正式公司备份。
- 新增小型 T5 浏览器 runner，连接同一候选包的隔离同源服务，用本轮合成夹具，走一次性票据→Cookie→实际 JS。核对五页、两个 HTML 入口、路由刷新、一次切公司/月份后的上下文、分页及页外数字凭证深链、历史/当前状态与非空员工/资产资料，并检查页面和控制台错误。仅补真实运行接缝，不重做完整竞态、非法参数和视觉重设计矩阵。
- 旧 v1 browser mock 脚本标为历史适用，在现行说明中指定新 runner；不整批重写旧脚本。未变化的前端单元/SSR 有效结果复用；若发现产品修复，再运行其受影响测试。

## 3. 必要读取成本与完整性验证

新增独立 T5 探针，复用旧 measure/grow 方法但不执行会覆盖 T4 原结果的旧 main；运行结果写 T5 专属文件。只做两组增长对照和一个控制样本：
- 四页同一合成库小→大：历史资金来源 48→480、jobs 3→30、audit 82→820；固定 2026-01、limit=1，各次使用新的连接、事务和 QueryReads。确认无关完整 outcome/事实装载为零、只展开页内来源，完整汇总和计数正确；历史现金聚合随真实相关业务增长并计入新增金额。
- 报表分类专项：48→480 条上一年真实凭证及适用分类，固定 2026 Q1 报表和 2026-03 readiness；有效全集作对照，再在集合末端加入一条无效引用，证明不能被分页或年份裁剪遗漏。允许必要标量元数据、逐项校验及问题输出增长；不得因此装载全部历史分类对象、outcome 或完整凭证行集合。不重跑 r2 全部错误组合。
- 来源错配控制：复用 r2 的声明 A/冻结 B 合成损坏副本，固定 A 的 current 清偿页 limit=1 与 summary；无关历史增长后同一 unresolved 记录、null 金额及精确 B/payment 追溯仍可见且不重复计数。不扩展为新的 A/B/并集全矩阵。

记录 SQL 数、SQLite VM（progress interval=100 估算）、JSON/对象装载、Python 请求峰值、响应字节及主要查询计划；连接设置与请求体分开。验证单响应公司库快照一致、不以跨请求或跨数据库原子性作承诺。耗时仅作描述，不设绝对延迟或固定 VM 比率门槛；对无关历史不允许按新增行逐条查询/完整解码，对相关增长解释必要成本。

合成库构造和读取结束后在测量区间外显式验证 schema 与完整引用多重集合，确认本次实际写入→目录→读取链。默认复用 v9→v10 回滚、旧版本迁移矩阵及局部命中/显式遗漏检测的有效证据；只有相应实现发生修改才补其定向回归。完整性验证不移入普通连接或页面请求。

## 4. 遗留问题处置、接口与文档收口

默认没有新的公共 API、响应版本、业务规则或数据库结构；只补验证资产、必要现行说明及已批准合同内的最小缺陷修复。确认不符合已批准合同的实现问题时，保留最小复现→修复→受影响验证及新代码哈希；涉及核算等价、采用证明、清偿归属、准备阻断、必填事实、数据库合同或任务范围的实质取舍，先回架构会话批准。

更新 README/现行 local-kernel 与打包说明的模块边界、v10/v3、版本化内容/时间、统一查询/分页、成本与完整性责任；由四份已审核专题合同作细则来源，避免复制另一套规则。旧软件包、旧验收和前序任务记录保留原数量/哈希/日期，仅补历史适用标识及 T5 结果链接。roadmap/task-prompts 属架构会话范围，T5 回传更新建议，由架构最终审核时更新状态。

遗留清单逐项记录已修复、已批准限制、阻塞/待决及独立后续范围。保守未知、必要历史成本、普通命中校验不能发现任意遗漏均是明确限制，不写成“修到就绪”的缺陷；真实规模和视觉重设计不作本次完成宣称。

## 5. 交付、准入与审批

批准后才落盘 docs/t5-approved-plan.md，最终交付 docs/t5-implementation-result.md 和 docs/t5-implementation-verification.json，附候选软件包/构建清单、精简成本表、精确运行 nodeid/命令、每批代码状态、复用证据及未决事项。证据 JSON 记录最终源码和测试哈希，避免自引用；文档完成后再记录独立最终清单。若中途代码变动，只按影响补验并注明适用性，不将多批通过数相加为一次最终结果。

结果送审条件：选定回归与集成实际通过；无未解释的金额/状态/来源差异、就绪误判、完整性失效或读取成本倒退；候选包与验证源码/前端/结构版本一致；全部未决问题明确交架构取舍。完成后主动回传本任务 ID、计划版本、交付范围、证据、限制及候选包标识，等待架构会话最终审核，不自行宣布项目已验收。

持续边界：只写本地主仓库批准范围，保留前序改动；不访问真实业务库、原件或负责人凭据，不启动已安装业务服务，不部署，不生成正式业务备份，不自行提交/推送或创建后续任务。临时服务、票据及测试凭据仅限 T5 隔离资料根，结束后撤销并停止。架构明确批准与退出计划模式均具备后才执行。

## 架构审批补充（已批准，无需 v2）

架构会话已明确批准 v1，用户切回默认模式并要求开始后实施。以下为批准时限定的执行边界：

1. 先补齐测试、验证器和浏览器 runner，再执行一次 release 构建和一份默认完整验证候选包。浏览器复用同一候选产物；路径、锁文件、源码、实际 JS 和迁移合同以文件摘要对应。
2. 打包前窄预检输出目录、ZIP、-relocated、-relocated-validation、-verification.json 五项，均使用全新的 T5 专属名字且不存在。保留旧产物，任何清空或移动先核验绝对路径；release 仅更新已知静态构建目录。
3. 默认验证临时 Windows 凭据及短暂原生测试窗口已获授权，仅限隔离测试身份。所有 CLI/MCP 显式指定 T5 --root，浏览器禁止回退默认 data/已安装服务；注销、删除测试会话凭据并停止服务。
4. 明确断言候选创建及恢复公司库 v10、目录 v3，且新模块/合同入包。合成备份不是正式备份或真实迁移验收；不扩大重验非空关账目录、所有任务目录或 v9 升级矩阵。
5. 选定去重节点单批执行，失败只补实际影响。成本限两组增长与一个控制，尽量共享一次资金/jobs/audit增长；不以恒定总 VM 或扩大通过数量作准入。
6. T4 r2 及公司 v10 已正式验收；结构指纹 0d7a1b1823d09b3f05b8a6f30b1dace1f1cc3dd83d1f0aa11260dacbde211f05 按冻结合同保持。可同步 AGENTS/现行文档冻结范围，实质性结构或核算、采用、清偿、阻断取舍仍须架构批准。

roadmap/task-prompts 由架构会话更新。T5 仅实施获准本地主仓库范围，不操作正式资料、不部署、不启用真实服务、不自行提交推送或创建后续任务。完成结果仍交架构会话审核。
