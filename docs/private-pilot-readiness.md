# 正式私有试用前准备度记录

> 状态：**已到达正式试用前停止线；不得导入真实业务数据、启用正式试用或部署生产环境**
> 记录日期：2026-08-11
> 代码基线：`62b4a40d1b522732766093de80c49363254e8bf6`
> 决策入口：[会计期间与月结产品决策记录](./accounting-period-close-decisions.md)
> 迟到证据基线：[迟到银行外部证据开发基线](./late-bank-evidence-development-plan.md)

## 1. 当前裁决

内核已能在隔离临时 PostgreSQL 17 上使用完全虚构数据演练“从过去月份开始，逐月录入、生成期间和关账”的流程，但这只是工程验证，不是正式试用授权。

用户已明确选择 `DEC-013 B`：关闭月的后到银行流水应作为迟到外部证据，不改旧关闭快照，只在后续开放月留下处理轨迹。审计又发现去重身份、只追加证据的匹配以及失败导入保留粒度会直接影响正式产品的数据正确性和隐私，已分别记录为 `DEC-020` 至 `DEC-022`。在得到用户填写并主动通知继续前，不实施该生产写入链。

## 2. 已通过的虚构数据演练

新增单一 PostgreSQL 演练用例 `tests/test_private_pilot_simulation_postgres.py`，且只连接由 Testcontainers 临时启动的 `postgres:17-alpine`。演练使用虚构企业、客户事实、员工、资产、贷款人、证据和银行行，未读取默认 Compose 数据库或真实文件。

演练已验证：

1. 从 2026 年 3 月开始，按顺序生成并关闭 3 至 7 月五个自然月；
2. 3 月正式事实包含服务现销、工资计提、固定资产取得/启用、无形资产取得/摊销、金融机构借款放款/计息及税期确认；
3. 4 至 7 月逐月完成固定资产折旧、无形资产摊销和借款计息后关账；
4. 8 月开放期中，税期来源锁先阻止直接冲正 3 月销售；按顺序先冲正税期调整、再冲正销售后，3 月关闭 JSON 和 SHA-256 快照逐字节不变；
5. 全部凭证由既有固定模板生成并借贷平衡，未新增自由分录入口。

精确执行结果：`1 passed in 12.31s`。演练完成后 Testcontainers 已自动清理，当前仅保留原有默认 `ai-accounting-core-postgres-1`，且本阶段没有写入它。

上一已推送基线的完整门禁为 `353 passed`，其中 PostgreSQL 17 集合 `109 passed`，总覆盖率 `86%`，Ruff、`pip check`、`compileall`、Alembic 单 head 和 `git diff --check` 通过。本轮未改产品代码，只增加演练测试与决策/状态文档。

## 3. 正式试用前仍必须关闭的门禁

| 范围 | 当前状态 | 为什么不能带真实数据进入 |
| --- | --- | --- |
| `DEC-013` 迟到银行证据 | B 已决定，契约已冻结，生产写链未实现 | 现有导入没有迟到标记、导入动作审计、关闭月共锁和完整处理链。 |
| `DEC-014` 银行对账深度 | 以决策文档中的填写和用户后续通知为准 | 现有“已复核”不证明对账单覆盖期、期初/期末余额和差异。 |
| `DEC-015` 真实身份与授权 | 待决定 | MCP 继承本机进程权限，确认人仅为文本，无法证明谁有权查看证据、记账或关账。 |
| `DEC-016` 部署、加密与文件边界 | 待决定 | Compose 仍是开发默认凭据；证据文件明文落地，导入路径没有正式白名单。 |
| `DEC-017` 备份恢复与退出 | 待决定 | 没有加密备份、恢复演练、保留期和试用结束后的交付/处置证据。 |
| `DEC-018` 专业会计/税务复核 | 待决定 | 内核不是税务意见或法定账簿，必须有专业人士复核企业配置、政策和首月结果。 |
| `DEC-019` CI 与依赖安全 | 待决定 | 当前全量门禁依赖本地人工执行，没有每次推送的自动证据、依赖/密钥扫描和 SBOM。 |
| `DEC-020` 至 `DEC-022` 迟到银行分支 | 待决定 | 它们分别决定已入账流水的证据关联、缺流水号去重和失败输入的隐私保留。 |

## 4. 历史账接入的硬边界

已决定的 `DEC-003` 允许企业从过去的真实开始记账月份起，逐月生成期间并录入截至当前的业务，但前提是能从该月起完整重建所有业务事实、证据和义务链。

若开始月之前已存在银行余额、应收应付、预收、固定/无形资产、借款、工资累计或税务累计状态，当前内核不得用自由分录、推断默认或伪造业务事件吸收这些期初量。这类企业必须停止接入，等待独立的期初余额/历史迁移项目和专业复核。

## 5. 继续条件

只有在用户完成决策文档中的相关选择并主动通知继续后，才会分包实施迟到银行证据、对账、身份授权、部署安全、备份恢复、专业复核和 CI 门禁。后续实施完成后还需使用新的完全虚构数据演练、独立终审和恢复演练复验。

即使上述门禁全部通过，仍须在任何真实文件、真实企业配置或真实业务事实进入前再次明确向用户报告，并等待用户给出启动正式试用的新授权。
