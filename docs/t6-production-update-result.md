# T6 正式环境更新结果 production-v11-r1

任务：01a095d6-05dd-7c63-82eb-d29543bf6c48。架构审核：01a09416-a4e3-76b2-bf46-6387d8d47cfc。用户已明确要求更新正式环境；v11-r1 候选及 cutover-r2 均在架构明确放行后执行。本文记录实际部署，最终验收由架构任务确认。

正式看板已更新为已验收的 T6 r2，两家公司的读取已恢复。受控新服务运行构建为 `local-kernel-2:e8947512b388fb25714251b9459d5b697f3cf010fb95731409ea746ecccac1e8`；目录库仍为 v3，两家公司均为 v11。

## 实际范围与效果

- 经营简报、资金、员工、资产、财务报表继续使用已验收的 T6 r2 页面。正式发布的 20 个静态文件与 r2 构建逐字节一致；已有旧页面使用的 22 个哈希资源继续保留。发布采用隔离 outDir，没有在正式目录直接清空构建。
- 首次正式读取发现两公司保存了有 T4 历史证据的中间 v10 结构；其实际结构与已登记 history10 同为 `45dd0bba8f9668b552859ff7beea612a8c51fd9838f1a3d9785c76771bc08266`，比最终冻结 v10 缺四个读取索引。严格检查曾拒绝公司读取，该失败证据保留于临时部署目录，没有伪装成成功。
- 经架构审核，增加独立完整历史合同及 v11 前向迁移。仅精确匹配该旧结构、身份和原登记的库可通过迁移前检查；标准 v10 同样有正常前向路径。公司 v11 补齐四个索引，旧 history10 保持，追加 history11；冻结 v1–v10 与目录 v1–v3 共 13 个合同文件逐字节不变。
- 本次新增或更新 12 个候选文件：4 个内核/合同文件、3 个定向验证测试、3 个未来验证脚本和 2 个版本说明。具体清单及部署前后 SHA256 见同名 JSON。另新增本结果说明及 JSON；未改写 T6 v1/r2 或 T4/T5 历史验收证据。

## 正式切换与内容保持

执行前核对目录与各公司库的公司 ID、数据库 ID、税号，核对两个路径唯一且属于已保全目标，并复核两库实际结构和原 history10 均为已审的精确旧形态。两公司 pending/running 任务与目录 pending operation 均为 0。

通过现有受保护元数据定位正式服务，调用停止并等待原 resident 进程退出，因 worker 是同进程线程，其退出一并得到确认。持有现有 instance_lock 后，再次检查目标及任务，取得目录和两公司的最终一致 SQLite Online Backup 维护快照，并保全原源码。快照使用现有 owner/SYSTEM 文件保护；私有公司路径映射不进入源码。这些是维修快照，不是正式公司 ZIP 备份交付。

持锁切换完整已审 v11 文件后，由全新受控进程只读计算主仓完整构建指纹；在任何公司迁移前确认与候选一致。两个公司库分别执行事务升级，锁内全部验证成功后启动全新主仓进程。没有执行业务事实写入或重新核算。

每个公司有 188 张表，升级前后 186 张业务表的完整内容摘要一致；identity 仅结构版本改变，schema_history 只追加 v11，原 v10 登记逐行保持。新增四个索引，严格结构、SQLite 完整性、外键和完整引用目录校验通过，二次升级为 noop。实际浏览器读取后再次只读核对，两公司所有表仍与刚完成迁移的状态一致，pending/running 仍为 0。

## 必要验证及证据

- 隔离候选：24 项定向核心测试、2 项现有读取索引前向迁移测试、1 项合成便携备份恢复集成测试通过；两家维修快照的独立克隆升级通过。修改 Python 的 Ruff 与辅助脚本语法检查通过。
- 正式 HTTP：六个入口路由、18 个当前资源及 22 个保留旧资源均校验内容 SHA256 与 no-store 响应。T6 r2 应用源码哈希和 v11 已审源文件哈希保持匹配。
- 实际 Edge 浏览器：复用已授权负责人登录，看到两家公司和经营简报四张核心卡；依次进入资金、员工、资产、财务报表并返回简报，全部读取成功；切换第二家公司后简报读取成功。10 个实际加载的 JS 文件哈希符合已验收构建，页面异常和 API 错误均为 0。未保存真实业务页面截图、内容或身份资料，未修改或撤销负责人凭据。
- 详细安全结果与证据哈希在 `docs/t6-production-update-result.json`；底层部署日志、辅助脚本、完整逐表摘要和维修快照位于忽略目录 `.tmp/t6-production-update-20260912/`。

未重跑内核全量、历史成本矩阵或完整软件包链；没有生成新的软件交付包、提交推送或清理原始资料。既有非阻断 server-disconnect P3 WinError10053 未在本次扩大修复，不能据此次浏览器无异常推断历史服务日志没有该问题。没有新增阻断使用的问题。
