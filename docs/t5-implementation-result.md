# T5 整体验证与交付收口 v1

任务 `01a095aa-d868-7bb2-a092-0ff76cc90108`；架构审核任务 `01a09416-a4e3-76b2-bf46-6387d8d47cfc`。
依据[已批准 v1 及六项执行边界](t5-approved-plan.md)实施。状态：实施验证完成，提交架构整体审核；本记录不自行宣布整个项目验收。

## 完成范围与适用代码

- 扩展原有工资补证—复核—关账—真实更正链，贯通共同业务状态、期间准备、简报、员工、资金及精确凭证追溯；经营说明原链增加页面内容有效性及冻结/后补分区检查。
- 补齐当前软件候选包的 CLI/MCP/HTTP 合同断言、同源浏览器验证及独立读取成本探针。旧 v1 浏览器 mock 标明历史范围。
- 更新现行模块、版本、构建入口、历史/当前时间、完整性和成本说明；旧包和旧验收的原数量、日期与哈希保持。
- 未修改生产 Python/Vue 实现、公共 API 或迁移合同。公司 v10、目录 v3；v10 结构指纹仍为 `0d7a1b1823d09b3f05b8a6f30b1dace1f1cc3dd83d1f0aa11260dacbde211f05`。目录 v1—v3、公司 v1—v10 按已验收冻结合同声明。

HEAD 为 `66561ee07afb2841562c269a9c60f320787d17e3`，前序改动仍未提交。交付依据是工作树及实际产物哈希；[机器可读记录](t5-implementation-verification.json)保留开工基线、各批状态、T5 增量及源码/实际 JS/合同与候选包逐文件对应，不用 HEAD 或页面 dirty 标签代替。

## 本次验证与复用

| 检查 | 实际结果与范围 |
| --- | --- |
| 统一 Python 节点 | 初批 26 项，24 通过、2 项新增夹具/表示断言失败。将精确引用 list/tuple 统一为 canonical 比较，并在说明夹具中用类型化一月事实建立合法页面月份后，仅补跑失败的 2 项，均通过。两批单独保留；不称为一次 26 项全通过。 |
| release 构建 | 一次 `build:release`，内含类型检查，通过。没有重复普通 build 或未变前端单元矩阵。 |
| 唯一软件候选 | 默认完整移目录验证通过；4,131 文件、16 次 CLI、真实 STDIO MCP 两个共同查询、七个鉴权 HTTP 查询、v10/v3 创建及恢复、原生测试窗口/测试身份、后台合成备份恢复。 |
| 同一候选浏览器 | 两公司、两月份、五页、双 HTML 入口、刷新、103 张月度凭证分页和页外数字链接、非空员工及资产通过；实际 Cookie/JS/API，前端错误和 API 失败列表为空。保留桌面及移动截图，未进行视觉重设计验收。 |
| 读取成本 | 一次综合探针的两组增长对照及一个错配控制全部通过，见下节。 |
| 静态检查 | T5 五个 Python 验证/测试文件 Ruff 通过；新增浏览器脚本语法检查通过；差异空白检查通过。 |

复用 T1 未变纯核算投影、T2 已有效来源追溯、T3 审核修订、T4 r2 三项缺陷、请求世代/分页/未知渲染、48 组合凭证差分、旧版本迁移/回滚与完整性责任边界证据。没有运行整个内核或全仓库矩阵，没有用历史批次数量累加替代本次跨层验证。

## 读取成本与限制

同一合成库历史资金来源 48→480、jobs 3→30、audit 82→820，固定 2026-01、limit=1。新请求、连接、事务与 QueryReads 分别计量：

| 页面 | SQL 数 | SQLite VM 估算 | Python 请求峰值（字节） |
| --- | ---: | ---: | ---: |
| 经营简报 | 299 → 299 | 64,400 → 254,200 | 937,040 → 912,715 |
| 资金 | 209 → 209 | 41,900 → 231,700 | 605,564 → 723,708 |
| 员工 | 237 → 237 | 22,100 → 22,200 | 584,482 → 583,850 |
| 资产 | 236 → 236 | 22,400 → 22,500 | 557,141 → 556,533 |

四页 JSON 解码及精确缓存数量不变，无关完整 outcome 和实体装载为零。资金余额 24,760→67,960 分，新增 43,200 分与真实新增资金业务一致；相应历史 SQL 聚合保留必要增长。

分类专项使用 48→480 条上一年真实凭证及适用分类。季度报表 SQL 保持 37，readiness 保持 41；报表 VM 13,900→133,000，Python 请求峰值 167,188→892,987 字节。完整报表事实引用 50→482（含报表口径及税务确认）；仅缓存 2 个必要事实，历史计算和凭证行缓存为零。末端追加缺失引用后，完整引用为 483，报表与 readiness 均返回问题，未截断分类校验。

独立错配控制保留声明 A/冻结 B 的精确来源：2 条相关清偿 slot 不重复计数，本页返回 1 条 unresolved，A 已付及余额保持 null。它是复用 r2 合成损坏副本的单控制，没有额外宣称错配路径做过增长矩阵。完整性核验在各规模组、分类样本及错配控制前后执行，均在计量区间外。

VM 使用 interval=100 的 progress handler 估算；Python 峰值不含 SQLite 原生内存与 OS 缓存，连接设置另记，不是冷启动或真实数据规模延迟承诺。完整分类、真实现金、相关清偿和完整冻结图允许必要输入/输出增长，不能用伪造确定状态或截断校验换恒定成本。

成本数字绑定实测脚本原件。测量后仅补异常时保留已测样本的记录逻辑，核心测量/增长函数 AST 不变，模拟异常验证通过；保留独立 addendum，不宣称对这份记录补丁重跑过成本。

## 候选软件与遗留取舍

候选 [t5-review-v1-20260912.zip](../.tmp-local-distribution/t5-review-v1-20260912.zip)，34,075,418 字节，SHA256 `862b55159d27ec833e26733db28d5a50bd62de50fdcab6a59e379287217d9f62`。
构建 `local-kernel-2:e3c9361e2558ddb7cd97f34b4ff71e64e34004e49a398e86d0ff58200461d31e`，Python 3.12.13 / SQLite 3.53.1。122 个应用/静态/验证器文件已与候选逐字节对应，包括 24 个当前页面资源；锁文件、迁移合同、包 manifest 和实际加载 JS 摘要均入清单。

候选的五个输出路径在运行前全部确认不存在，保留旧产物；只构建/打包一次，浏览器使用同一 relocated 产物及另一显式隔离 root。所有验证使用合成公司，不访问真实业务资料或负责人凭据，不部署、不启用安装服务、不生成正式业务备份；测试会话注销并删除凭据、停止服务。包验证不代表完整旧版本迁移矩阵、所有非空 close/job/audit 场景或真实公司迁移已重验。

**P3 已由架构明确接受为非阻塞遗留：客户端断连后的响应/日志处理不足。** 浏览器导航过程中，只读 dashboard GET 已完成 dispatch，在发送响应时出现 WinError10053；统一异常处理又尝试写同一已断连接，留下第二次堆栈。原 stderr 保留在 `.tmp/t5-v1/candidate-browser.json`；退出码 0、前端/API 错误为空、后续断言和凭据清理成功，未见计算或写入失败证据。WinError10053 不被当作已经证明的唯一网络原因，也不宣称服务端零错误。架构决定本轮不扩展 HTTP 修复、不再次构建或新增测试批次；后续可在发送边界区分连接中断与业务异常，不另建任务。

没有待本任务决定的合同变更。保守冻结未知、必要历史成本、显式完整性验证才能发现任意遗漏，以及真实规模/视觉后续范围均保持原批准限制。整体验收结论和 roadmap/task-prompts 更新交架构会话。
