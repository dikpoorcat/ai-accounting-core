# T4 定向修订交付 r2

任务 `01a09521-a902-7ca2-9fbb-2d9ffa74f194`，提交架构任务 `01a09416-a4e3-76b2-bf46-6387d8d47cfc` 复审。范围为架构首轮审核直接放行的 R1、R2、R3；原 [r1 结果](t4-implementation-result.md) 和 [r1 验证记录](t4-implementation-verification.json) 保持原样。

| 审核项 | 修订结果 | 必要验证 |
| --- | --- | --- |
| R1 报表分类引用漏验 | 先读取全部适用分类的规范化标量引用，在同一 Python 校验路径处理引用不存在、期间、重复来源、行号及科目问题；仅报告明细需要的分类装载完整内容。精确候选及冲正复用共同选择器和冻结引用核验。 | 无效引用原问题已复现；新增缺失、未来期间、冲突、上一年分类及替换版本用例，覆盖正式期间准备核心与必要报表邻接场景。 |
| R2 数字凭证链接失败 | HTTP 查询解析器按已有整数参数规则转换 `voucher_number`，原严格类型及定位规则保持不变。 | 实际回环 HTTP：目标凭证在当前页外仍可定位；编号与版本 ID 精确一致；未知编号、非法值、重复值和冲突定位符保持错误含义。 |
| R3 来源错配清偿被遗漏 | 候选分页与共同 reducer 同时匹配声明来源 A 和冻结来源 B；保留原冻结 B 的精确追溯与 unresolved。A 的已有义务沿用未知金额处理，B 自身已知义务不被错误金额污染；并集不重复计数。员工与资产已有明细显式展示关系未确认。 | 合成损坏记录使用实际选择器、关系解析器和汇总路径，检查历史／当前 A、B、并集、分页、计数、null 与无关 C；页面定向渲染及类型检查／构建。 |

全部命令、各自结果、最终文件哈希和相对 r1 的增量索引见 [r2 验证记录](t4-review-r2.json)。各次运行保留独立状态：R1 邻接批次有一项新增夹具错误，修正夹具后仅重跑该项；不把其与其他批次相加为一次最终通过数量。

本次没有新增结构、修改 v10 合同或增加核算规则。已审核的迁移、完整性接缝、历史选择、有限期初采用证明、页面请求代次与成本证据按原适用范围复用，未重跑整套 T4 或全内核测试。

R1 完整的适用分类元数据、必要标量校验和问题输出仍随相关历史规模增长，不能截断。完整分类内容、计算结果及历史凭证行集合没有因此全量装载；r1 对真实历史现金聚合等剩余成本的说明仍适用。

验证仅使用仓库虚拟环境、临时合成公司、回环测试 HTTP 和前端本地构建；未访问正式公司资料、启动安装服务或部署。r2 完成结果等待架构复审，T5 仍由架构任务安排。
