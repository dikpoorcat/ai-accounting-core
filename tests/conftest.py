"""Default tests exercise the standalone SQLite kernel without legacy imports.

The previous PostgreSQL fixture is retained in legacy_conftest.py only until
the evidence-based reconstruction and recovery prerequisites permit retirement.
"""
